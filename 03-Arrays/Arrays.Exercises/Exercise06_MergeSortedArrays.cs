namespace DsaPractice.Arrays;

/// <summary>
/// Exercise 06 — Merge two sorted arrays into a new sorted array. Target: O(n + m). See README.md for details.
/// </summary>
public static class SortedMerger
{
    public static int[] Merge(int[] first, int[] second)
    {
        throw new NotImplementedException();
    }
}
