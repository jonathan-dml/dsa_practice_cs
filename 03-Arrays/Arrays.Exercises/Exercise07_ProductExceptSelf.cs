namespace DsaPractice.Arrays;

/// <summary>
/// Exercise 07 — For each position, the product of all other elements, without division.
/// Target: O(n). See README.md for details.
/// </summary>
public static class ProductExceptSelf
{
    public static long[] Compute(int[] numbers)
    {
        throw new NotImplementedException();
    }
}
