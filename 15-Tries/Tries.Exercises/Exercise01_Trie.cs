namespace DsaPractice.Tries;

/// <summary>
/// Exercise 01 — A prefix tree supporting insert, exact search and prefix search. See README.md for details.
/// </summary>
public class Trie
{
    // Suggested design:
    // private sealed class Node { public Dictionary<char, Node> Children { get; } = new(); public bool IsWord; }
    // private readonly Node _root = new();

    public void Insert(string word)
    {
        throw new NotImplementedException();
    }

    public bool Search(string word)
    {
        throw new NotImplementedException();
    }

    public bool StartsWith(string prefix)
    {
        throw new NotImplementedException();
    }
}
