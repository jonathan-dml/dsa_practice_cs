namespace DsaPractice.Heaps;

/// <summary>
/// Exercise 03 — Track the k-th largest value of a stream. Target: O(log k) per Add. See README.md for details.
/// </summary>
public class KthLargestInStream
{
    public KthLargestInStream(int k, IEnumerable<int> initial)
    {
        // TODO: validate the arguments and add the initial values.
    }

    public int Add(int value)
    {
        throw new NotImplementedException();
    }
}
