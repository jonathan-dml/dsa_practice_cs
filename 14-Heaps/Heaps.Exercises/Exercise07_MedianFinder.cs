namespace DsaPractice.Heaps;

/// <summary>
/// Exercise 07 — Median of a growing collection using two heaps. See README.md for details.
/// </summary>
public class MedianFinder
{
    // Suggested fields:
    // private readonly PriorityQueue<int, int> _lower = new(Comparer<int>.Create((a, b) => b.CompareTo(a))); // max-heap
    // private readonly PriorityQueue<int, int> _upper = new();                                              // min-heap

    public int Count => throw new NotImplementedException();

    public void Add(int value)
    {
        throw new NotImplementedException();
    }

    public double Median()
    {
        throw new NotImplementedException();
    }
}
