namespace DsaPractice.HashTables;

/// <summary>
/// Exercise 02 — Find two indices whose values add up to the target. Target: O(n). See README.md for details.
/// </summary>
public static class TwoSum
{
    public static (int First, int Second)? Find(int[] numbers, int target)
    {
        throw new NotImplementedException();
    }
}
